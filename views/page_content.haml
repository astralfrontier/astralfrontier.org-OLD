%article
  - if @page.metadata("Icon")
    %img{src: @page.metadata("Icon"), align: "left"}
  ~ @page.to_html(self)
  = haml :page_meta, layout: false, locals: { page: @page }
  %section.pages
    = article_summaries(@page.pages)
  - unless @page.articles.empty?
    %section.articles
      %header
        %h1= articles_heading
      = article_summaries(@page.articles)
